# Snapshot binaries (kept in GitHub Releases only)
server/data/**/*.json.gz
server/data/**/*.bin.gz

# Go build output
server/bin/

# WASM build output (kept in GitHub Releases only)
server/wasm/leanimt-plus.wasm
server/wasm/wasm_exec.js
server/wasm/tree-snapshot.bin.gz

# OS
.DS_Store
